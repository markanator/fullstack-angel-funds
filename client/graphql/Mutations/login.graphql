mutation Login($password: String!, $email: String!) {
  login(email: $email, password: $password) {
    errors {
      field
      message
    }
    user {
      id
      fullName
      avatarUrl
      email
      created_at
    }
  }
}
